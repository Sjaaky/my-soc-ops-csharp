@using SocOps.Models

<div class="ocean-board-wrap w-full max-w-md mx-auto">
    <div class="ocean-badge mb-3">Saltwater board</div>
    <div class="grid grid-cols-5 ocean-board w-full max-w-md mx-auto aspect-square">
        @foreach (var square in Board)
        {
            <BingoSquare
                Square="@square"
                IsWinning="@WinningSquareIds.Contains(square.Id)"
                OnClick="@(() => OnSquareClick.InvokeAsync(square.Id))" />
        }
    </div>
</div>

@code {
    [Parameter]
    public List<BingoSquareData> Board { get; set; } = new();

    [Parameter]
    public HashSet<int> WinningSquareIds { get; set; } = new();

    [Parameter]
    public EventCallback<int> OnSquareClick { get; set; }
}
